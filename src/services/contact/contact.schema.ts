/**
 * Contact Form Validation Schema
 *
 * Re-exported from unified schema at @/db/schema for backward compatibility.
 */

export {
  ALLOWED_CONTACT_REASONS,
  type ContactReason,
  ContactFormSchema,
  type ContactFormInput,
  validateContactForm,
} from "@/db/schema";
